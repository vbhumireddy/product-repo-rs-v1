/**
 * 
 */
package poc.config;

import org.springframework.context.annotation.Configuration;

/**
 * @author bhumireddy
 *
 */
@Configuration
public class MongoConfiguration /*extends AbstractMongoConfiguration*/{

	/*
	 * @Override public MongoClient mongoClient() { Fongo fongo = new
	 * Fongo("mockDB"); return fongo.getMongo(); }
	 * 
	 * @Override protected String getDatabaseName() { return "mockDB"; }
	 */
}
